---
- name: Install packages
  become: yes
  apt:
    update_cache: yes
    name:
      - simple-scan
      - thunderbird

- name: Install flatpaks
  become: yes
  flatpak:
    name: "{{ item }}"
  with_items:
    - com.spotify.Client
    - org.qbittorrent.qBittorrent
    - tv.kodi.Kodi
