---
- name: Install packages
  become: yes
  apt:
    update_cache: yes
    name:
      - git
      - gitk
      - rclone
      - ripgrep
      - rsync
      - tmux

- name: Install PIP packages
  become: yes
  pip:
    executable: pip3
    name:
      - ansible
      - ansible-lint
