---
- name: Install packages
  become: yes
  apt:
    update_cache: yes
    name:
      - gnome-session
      - gnome-shell-extension-appindicator
      - gnome-tweaks
      - gnome-shell-pomodoro
      - nautilus
      - chrome-gnome-shell
